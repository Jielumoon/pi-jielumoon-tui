# 调查证据

- 用户确认 write 正常，edit 的红绿差异显示出现交错、闪烁、卡闪；尚无录屏或精确触发参数。
- docs/work.md 已有折叠预览补齐 8 行的高度修复；本轮需区分高度变化与预览内容重排，不能直接沿用旧结论。
- 宿主 TUI 文档要求 render(width) 每行不超宽，状态改变后 requestRender；主题变化 invalidate 不应推进动画。
- 16 路只读调查已派发，结论待源码复核及实际复现。
- 主模型直接运行 EditCallComponent 复现：newText 10 行先播完，逐步补 oldText 时，update 把已揭示文本裁回共同前缀（只剩 replace/少数红行），后续帧再播绿行；下一次 oldText 增量再次倒带。固定 9 行并不能阻止内容反复清空。
- 源码 edit-stream.ts：输入固定 label/remove/add 排序；update 对非前缀内容回退重播。候选最小修复：非尾部追加改为直接展示新快照，不重播；正常尾部追加仍逐字动画。不引入字段级动画队列。
- 用户要求删除旧闪屏补丁脚本及其活跃入口；历史工作记录保留，不修改全局 Pi 或备份。
